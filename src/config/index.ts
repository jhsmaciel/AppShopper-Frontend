export const nameStorage = "@AppShopper:";

export enum colors {
    primary = "#C1559D",
    light = "#FFFFFF",
    disabled = "#ECECEC"
}

export enum sizes {
    big = 22,
    medium = 16,
    small = 13,
}